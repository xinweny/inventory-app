extends layout

block content
  main
    div
      h1 Songbird Instruments
      p Welcome to #[em Songbird Instruments], an inventory management app for musical instruments.

    div
      p Our current inventory includes:
      div 
        p= `${count.brands} Brands`
        p= `${count.instruments} types of Musical Instruments`
        p= `${count.models} Instrument Models`